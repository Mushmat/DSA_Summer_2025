// Checks if the array is in non-descending order
// Start from the first index and check with the previous, is it smaller or equal. 
// Keep going next, if you find an element whose previous is bigger, than return False
// Else true
// Very simple, takes O(n) time.
